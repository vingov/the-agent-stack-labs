# Expected checkpoints

Expected results are hypotheses to test, not answers to force.

Use the [claim matrix](claim-matrix.md) to compare a run with the Windows reference. A mismatch may indicate:

- version drift
- operating-system behavior
- provider-specific behavior
- different context-engine behavior
- insufficient instrumentation
- a flaw in the reference claim

Preserve unexpected results and classify them accurately.
